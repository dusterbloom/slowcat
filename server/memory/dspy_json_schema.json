{
  "type": "object",
  "properties": {
    "relations": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "subject": {
            "type": "string",
            "description": "The subject entity of the relation"
          },
          "predicate": {
            "type": "string", 
            "description": "The relationship type (e.g., owns, has_pet, breed_of, located_in)"
          },
          "object": {
            "type": "string",
            "description": "The object entity of the relation"
          },
          "confidence": {
            "type": "number",
            "minimum": 0.0,
            "maximum": 1.0,
            "description": "Confidence score for this relation"
          }
        },
        "required": ["subject", "predicate", "object", "confidence"],
        "additionalProperties": false
      },
      "description": "Array of extracted knowledge relations"
    }
  },
  "required": ["relations"],
  "additionalProperties": false
}