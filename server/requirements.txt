# Note: This project requires Python 3.12 or earlier due to mlx-audio dependencies
python-dotenv==1.1.1
fastapi[all]>=0.95.0
uvicorn==0.35.0
opencv-python>=4.12.0.88
mlx-lm>=0.24.1
mlx-audio==0.2.3
pipecat-ai[openai,deepgram,rime,silero,mlx-whisper]>=0.0.76
aiortc==1.13.0
nltk==3.9.1
spacy>=3.7.2
spacy-transformers>=1.3.5
bm25s>=0.1.10
resemblyzer>=0.1.3
beautifulsoup4>=4.12.0
html2text>=2020.1.16
# Note: The pytz package is in maintenance mode. For new projects on Python 3.9+, consider using the built-in zoneinfo module and the tzdata package.
pytz==2024.1
aiosqlite==0.21.0 

# MCP Proxy for tool integration with LM Studio
mcpo>=0.0.17

# browser tools
trafilatura==2.0.0
# Note: The 'tools' package name is generic. A package with this name exists on PyPI (version 1.0.4), but it's unclear if this is the intended package.
tools==1.0.4

# --- Sherpa-ONNX (new) ---
numpy>=1.26
sherpa-onnx>=1.12.8
onnxruntime>=1.22.1
huggingface_hub>=0.23.0

# --- DuckDuckGo Search ---
ddgs>=9.5.2 

loguru==0.7.3
transformers>=4.10.3