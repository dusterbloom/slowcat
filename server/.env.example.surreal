# Minimal Surreal-first configuration for Slowcat (server/.env)

# Core
APP_PORT=7860
LLM_MODEL="qwen/qwen3-1.7b"

# Optional: pin a consistent user id (empty = auto)
# USER_ID="peppi"

# SurrealDB (defaults work for local installs)
SURREALDB_URL="ws://127.0.0.1:8000/rpc"
SURREALDB_NAMESPACE="slowcat"
SURREALDB_DATABASE="memory"
SURREALDB_USER="root"
SURREALDB_PASS="slowcat_secure_2024"

# Enable embeddings on tape so semantic retrieval (KNN) works out of the box
SURREAL_EMBED_TAPE=true

# Logging (keep quiet by default; flip to true to debug)
LOG_LEVEL=info
STT_VERBOSE=false
STT_LOG_INTERIM=false
STT_LOG_PUNCT=false
TTS_VERBOSE=false
TTS_LOG_SANITIZATION=false

# Advanced (optional)
# SC_USE_ABSTRACT_SUMMARY=false
# SUMMARIZER_PROVIDER=lmstudio
# LMSTUDIO_BASE_URL="http://localhost:1234/v1"
# SUMMARIZER_MODEL="dolphin3.0-llama3.2-3b"

# Notes
# - SurrealDB memory is the default — no USE_SURREALDB flag needed.
# - Non-user facts are included by default; cap via FACTS_MAX_NONUSER (default 6).
# - To revert to legacy user-only facts, set FACTS_ONLY_USER_SUBJECT=true (not recommended).
